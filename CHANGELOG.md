# 小窗浏览器更新日志

## 1.4.0 - 2026-08-17

v1.4 是一次完整的界面、窗口模式、热键和媒体控制重构。配置文件、书签、标签会话和旧版预设保持兼容，缺失的新配置项会自动采用默认值。

### 界面与交互

- 使用 Direct2D / DirectWrite 重做 Fluent 风格 Win32 界面，统一圆角、悬停、按下、禁用和焦点状态。
- 跟随 Windows 深浅色，可手动选择主题；修复切换主题时部分栏目不刷新或残留旧配色。
- 新的蓝紫“悬浮窗口＋圆形挖孔”图标统一用于 EXE、任务栏、Alt-Tab、窗口和托盘。
- 压缩顶部浏览器栏高度并提高文字可读性；新建标签按钮与窗口按钮尺寸统一。
- 标签页根据可用空间自动收窄，保留拖动区域；关闭按钮直接显示在标签内部。
- 标签栏右键增加“关闭当前标签”“关闭其他标签”和“关闭全部标签”。
- 书签改成下拉式列表，保留快速访问、编辑和删除。
- 地址栏双击选中完整网址。

### 标签、书签与预设

- 修复切换或关闭标签后旧 WebView 画面短暂残留的问题。
- 关闭最后一个标签或新建标签后使用默认网址的真实页面标题，不再固定显示“主页”或“新标签页”。
- 预设保存并恢复全部标签页和当前标签，不再只保存最后一个页面。
- 预设名称去除首尾空格后不可重复；修复预设窗口文字裁切。
- 预设继续保存窗口布局、沉浸设置、热键和常用开关；书签保持全局共享。

### 窗口、全屏与沉浸

- 窗口状态统一为 Normal、Maximized、ImmersionHole、ImmersionAutoHide 和 Hidden，由单一状态机切换。
- 右上角最大化按钮改为无缝覆盖当前显示器工作区，移除边框、阴影和微量留白，同时保留浏览器顶部栏。
- 网页播放器全屏限制在小窗网页区域；非沉浸模式继续保留顶部浏览器栏。
- 新增“网页视频全屏时按画面比例调整小窗”选项，退出后恢复进入全屏前的位置和大小。
- 网页视频全屏期间可以切换沉浸模式，不再强制退出播放器全屏。
- 挖孔模式固定为圆孔完全透明、孔外不透明；透明度设置只在整体透明模式中显示。
- 整体透明模式只在系统光标真实可见时执行移入隐藏，并加入防抖，减少游戏锁定光标时误隐藏。
- 沉浸穿透样式集中设置，修复预设、托盘、恢复和快速连按导致的穿透丢失、白屏或异常退出。
- 沉浸切换使用单次布局和统一重绘，降低闪白、卡顿和位置跳变。
- 新增可选的“无缝最大化时从顶部空白拖动恢复”，默认关闭。

### 托盘、隐藏与置顶

- 修复任务栏最小化后无法拉起、隐藏恢复后网页空白以及最小化成为细条的问题。
- 托盘图标支持左键恢复；右键菜单提供打开、设置和退出。
- 修复焦点位于小窗内部时隐藏后必须点击其他窗口才能再次显示的问题。
- 修复第二次启动程序时无法恢复已存在实例的问题。
- 置顶在窗口恢复、模式切换和前台窗口变化后安全重申，不抢夺键盘焦点。
- 管理员清单改为 `requireAdministrator`，提高在管理员游戏和启动器中的热键可用性。

### 媒体控制与网站适配

- 媒体适配顺序为 Bilibili、抖音、YouTube、通用 HTML5 视频。
- 根据播放状态、可见面积和最近交互选择活动视频，并支持 SPA 页面和子框架。
- 点按快进/快退跳转 5 秒；按住 400ms 后进入连续快进或倒退，快进倍速可选 2×–5×。
- 松开按键、失焦、输入保护触发或页面切换时恢复原播放状态和倍速。
- 新增“隐藏时允许媒体热键”，可在窗口隐藏后继续播放、暂停、快进、快退和选集。

### 热键与输入保护

- 修复程序启动后必须先点击小窗才会注册热键的问题。
- 修复网页全屏期间沉浸热键无效、快速连按模式键导致隐藏或异常退出的问题。
- 键盘低级钩子作为 `RegisterHotKey` 的全屏游戏后备路径。
- 鼠标中键、侧键 1 和侧键 2 使用低级鼠标钩子与 Raw Input 双通道并自动去重。
- 修复管理员模式下部分鼠标驱动无法绑定或触发侧键的问题。
- 输入保护结合网页焦点、原生编辑框、GUI 线程信息和 UI Automation；Electron/Chromium 整块渲染表面不再被误判成输入框。
- 输入状态下暂停媒体和沉浸热键，同时始终保留隐藏/显示键。

### 性能与诊断

- 移除常驻 16ms 挖孔轮询，改为鼠标移动合并和约 30Hz 区域更新。
- 窗口隐藏且未启用后台媒体热键时暂停无用的 WebView 可见渲染。
- 新增自动 GPU 与软件兼容模式；软件兼容模式通过 `--disable-gpu` 启用并在重启后生效。
- 新增可复制诊断信息：Windows、HAGS、WebView2 Runtime、渲染模式、窗口模式、活动视频尺寸和丢帧统计。
- 修复结构体初始化、64 位句柄转换、字符串终止、COM 返回值、资源释放和退出顺序等问题。

### 架构与依赖

- 将原单文件实现拆分为应用状态、配置、主窗口、主题、窗口模式、热键、输入保护、媒体桥接和诊断模块。
- WebView2 SDK 从 1.0.3650.58 升级至 1.0.4078.44。
- 保持 C++20、Win32、WebView2 和便携文件夹交付方式，不引入 Windows App SDK 运行时。
- 新增无第三方测试框架的核心测试目标，并提供真实窗口输入与窗口模式冒烟脚本。

### 验证

- Release x64 与 x86 均通过 `/W4` 构建。
- 两个架构的 65 项核心测试全部通过。
- MSVC 静态分析的 20 个报告均为零缺陷。
- 窗口级测试覆盖启动热键、双侧键绑定与执行、主题切换、托盘恢复、地址栏全选、沉浸区域和快速模式切换。

### 已知限制

- 真正绕过 DWM 的独占全屏游戏无法被普通桌面窗口覆盖，请使用无边框全屏或窗口化全屏。
- 网站播放器结构可能更新；适配器失效时会回退到通用 HTML5 视频控制。
- 软件兼容渲染模式需要重启程序后生效。
- 便携版为兼容管理员游戏会在每次启动时显示 Windows UAC。

## 1.3

- 新增普通模式最大化、托盘模式、输入检测开关和鼠标中键/侧键绑定。
- 修复默认主页、会话恢复、多实例、全屏状态和快捷键绑定问题。

详细记录请查看 [v1.3 Release](https://github.com/azurplain/Mini-Window-Browser/releases/tag/1.3)。

## 1.2

- 新增场景预设、书签、整体透明沉浸模式、系统托盘和窗口全屏。

## 1.1

- 初始公开版本：挖孔穿透、全局热键、多标签、边缘吸附和会话恢复。
