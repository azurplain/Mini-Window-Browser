# 小窗浏览器用户手册

版本：v1.4.0 正式版
适用系统：Windows 10 / 11（x64 为主要测试目标）

小窗浏览器是基于 C++20、Win32 和 Microsoft Edge WebView2 的便携式画中画浏览器，主要面向只有一块显示器、需要边玩游戏边看攻略或视频的用户。

## 1. 安装与数据

将测试包解压到普通可写文件夹，双击 `XiaoChuang.exe`，并在 Windows UAC 提示中选择“是”。`XiaoChuang.exe` 与 `WebView2Loader.dll` 必须位于同一目录；系统还需要 Microsoft Edge WebView2 Runtime，Windows 10/11 通常已经安装。

已确认 ATK HUB 与《洛克王国：世界》的 WeGameLauncher 都请求管理员权限，因此正式版 EXE 清单使用 `requireAdministrator`。Windows 不支持“一次同意后永久免提示”，所以每次启动都会出现 UAC；这是为了让键盘和鼠标侧键热键与管理员游戏保持同一权限级别。提高权限并不能让普通桌面窗口覆盖绕过 DWM 的真正独占全屏游戏。

首次正常退出后会在程序目录生成 `config.ini`，其中包含设置、标签会话、书签和场景预设。测试包不会附带开发者的配置、Cookie 或浏览历史。备份 `config.ini` 即可备份便携配置；WebView2 网站数据位于运行时创建的用户数据目录。

## 2. 主界面

主界面由两层浏览器栏组成：

- 顶层是标签栏和拖动区，右侧依次是新标签、最小化、无缝最大化和关闭。
- 标签较多时会像常规浏览器一样统一收窄，不再显示左右滚动箭头；选中标签始终显示关闭按钮，宽度足够时其他标签也显示关闭按钮。
- 第二层是地址栏、前往、收藏、书签、预设和设置。
- 输入网址可直接访问；输入普通文本会使用 Bing 搜索。
- 单击地址栏获得焦点时会选中网址；双击地址栏也会选中完整网址，而不是只选择一个单词。
- 点击标签右侧的 `×` 区域关闭标签；只剩一个标签时会重置为主页。标签栏右键可关闭当前标签、其他标签或全部标签（关闭全部后保留一个主页标签）。

界面会跟随 Windows 深浅色，也可在设置中固定为浅色或深色。窗口和弹窗按当前显示器 DPI 缩放。

## 3. 窗口与全屏

主窗口始终尝试使用 `HWND_TOPMOST` 保持置顶，并在游戏切换、窗口恢复和模式切换后以不抢焦点的方式重申置顶。

“无缝最大化”会保留顶部浏览器栏，精确覆盖当前显示器工作区且移除缩放边框；再次点击恢复原位置。设置中的“拖动顶部空白可还原并移动”默认关闭，此时顶部空白不会改变窗口，只有右上角按钮能退出；开启后可从顶部空白拖动恢复并保存新位置。

网页播放器自身请求全屏时，内容只覆盖小窗的网页区域，不会扩展到整个显示器；非沉浸模式仍保留顶部浏览器栏。设置中的“网页视频全屏时自动按画面比例调整小窗大小”默认关闭，开启后会按活动视频比例临时改变普通窗口尺寸以减少黑边，退出网页全屏后恢复原尺寸。网页视频全屏期间按 `0` 进入或退出沉浸，只改变小窗窗口模式，不再退出网页全屏；只有 `Esc` 或播放器自己的退出按钮会结束网页全屏。

无边框全屏游戏通常可被小窗覆盖。真正绕过 DWM 的独占全屏渲染不允许普通桌面窗口叠加；本项目不会注入游戏进程或使用可能触发反作弊的覆盖层。

## 4. 沉浸模式

默认按 `0` 切换沉浸模式。两种风格都会移除浏览器栏并启用鼠标点击穿透：

1. 鼠标圆形挖孔：视频窗口保留，鼠标附近形成固定完全透明、可穿透的圆形孔洞，圆孔外的视频始终保持 100% 不透明。
2. 整体透明：鼠标移出时按设定透明度显示；只有系统光标真实可见且稳定进入窗口 80ms 后才完全隐藏。游戏隐藏或锁定光标时，视频会保持可见。

透明度参数只属于“整体透明”模式，范围 0–100%，默认 100%，表示鼠标移出时的窗口不透明度；选择圆形挖孔时该设置会隐藏。沉浸模式集中使用分层、透明和不激活窗口样式，点击应落到下层游戏。

## 5. 默认快捷键与长按

| 默认键 | 功能 |
|---|---|
| `0` | 切换沉浸模式 |
| `` ` `` | 播放 / 暂停 |
| `5` | 点按后退 5 秒；长按连续倒退 |
| `6` | 点按快进 5 秒；长按临时倍速播放 |
| `7` | 上一集 / 上一个视频 |
| `8` | 下一集 / 下一个视频 |
| `9` | 隐藏 / 显示窗口 |

按下快进或快退后，在 400ms 内松开会执行一次 ±5 秒跳转；超过 400ms 才进入长按。快进倍速可设为 2×–5×，默认 3×。松开、失焦、输入保护触发、导航或页面切换时会恢复原播放/暂停状态和原倍速。

所有热键都可改为键盘组合或鼠标中键/侧键。若组合已被系统或其他程序占用，会显示冲突提示。

## 6. 网站媒体适配

媒体控制依次选择 Bilibili、抖音、YouTube 的站点适配器，最后回退到通用 HTML5 `<video>`。页面有多个视频时，会综合播放状态、可见面积和最近交互选择活动视频。注入脚本会随顶层页面、SPA 导航和子框架文档创建。

网站改版可能使“上一集/下一集”的专用按钮选择器暂时失效；播放、暂停和时间跳转通常仍可通过 HTML5 回退工作。网页内输入框获得焦点时会向原生输入保护发送状态。

## 7. 隐藏、托盘与后台媒体

默认按 `9` 隐藏窗口。可以选择隐藏时自动暂停视频。

- “隐藏时允许媒体热键”默认关闭：隐藏后仅保留显示/隐藏热键，并暂停无用的 WebView 可见渲染。
- 开启后：播放/暂停、快进、快退、上一集和下一集仍全局可用；沉浸切换仍保持禁用。
- 开启“最小化时隐藏到系统托盘”后，最小化会隐藏；关闭时则正常进入任务栏。
- 托盘图标左键单击会打开小窗；右键菜单提供打开、设置和退出程序。

## 8. 输入保护

“输入文字时暂停全部全局热键”默认开启。软件结合以下信号判断输入状态：

- 地址栏和网页 `focusin` / `focusout`；
- 前台 GUI 线程的焦点与光标信息；
- 常见 Edit、RichEdit、Scintilla、TextBox 类名；
- 同时具备编辑语义、键盘焦点和可写 Value 模式的 UI Automation 控件。

检测到网页输入、游戏聊天框或其他程序编辑控件时，媒体和沉浸热键会暂停，离开输入状态后再恢复；“隐藏/显示”始终保留，避免窗口隐藏后无法拉起。Electron/Chromium 的整块渲染表面不会仅凭“像 Edit”或“带可写 Value”就触发保护。正式测试包默认以管理员权限运行，通常可与管理员游戏保持同一权限；若某个游戏使用完全自绘且不暴露辅助功能的输入框，仍可能无法识别。

## 9. 书签与场景预设

星形按钮可快速收藏或取消收藏当前页面。书签窗口可编辑标题和 URL、双击打开、删除或新增。

场景预设保存窗口位置与无缝最大化状态、整体透明模式不透明度、挖孔半径、吸附距离、沉浸风格、热键、隐藏自动暂停、输入保护、托盘、后台媒体热键、长按倍速、主页，以及保存时的全部标签页和当前标签。应用预设会用保存的整组标签恢复标签栏，但不会删除书签。预设名称会去除首尾空格，并且不允许与现有名称重复。主题、GPU 渲染模式和最大化顶部拖动开关是应用全局设置，不随预设切换。

点击任一快捷键按钮后，可直接按键盘组合键、中键或鼠标侧键；捕获期间旧热键会暂时停用，按 `Esc` 取消。中键和两个侧键同时通过低级鼠标钩子与 Raw Input 识别，并会自动去重。普通网页正文不会被误认为输入框，只有网页输入元素、原生编辑框和可写辅助功能控件会触发输入保护。

程序启动时会先注册热键，再启动输入保护。Windows 若阻止新窗口抢前台，启动前遗留在其他程序输入框中的焦点不会误停用小窗热键；启动后真正发生新的外部焦点事件时，跨程序输入保护才开始判断。键盘热键还使用低级钩子作为全屏游戏后备，但游戏与小窗权限级别仍应一致。

## 10. GPU、HAGS 与诊断

性能设置提供：

- 自动 GPU（默认）：使用 WebView2 默认 GPU 合成路径。
- 软件兼容模式：向 WebView2 传入 `--disable-gpu`，重启后生效，仅用于排查兼容性或与 HAGS 的组合问题。

沉浸挖孔仅在光标位置改变时更新区域，并限制为约 30Hz，避免旧版常驻 16ms 强制重绘。进入和退出沉浸会先一次性更新窗口样式、位置、浏览器栏与挖孔区域，再统一重绘，避免中间帧造成的白闪和位置跳变。窗口隐藏且未开启后台媒体热键时会暂停 WebView 可见渲染负载。

设置页的“复制诊断信息”会收集 Windows 构建号、HAGS 注册表状态、WebView2 Runtime 版本、渲染模式、窗口模式、透明度、活动媒体尺寸以及 `droppedVideoFrames` / `totalVideoFrames`，并复制到剪贴板。测试掉帧时请分别记录 HAGS 开/关与自动 GPU/软件模式结果。

## 11. 常见问题

### 游戏全屏时看不到小窗

先尝试游戏的无边框全屏或窗口化全屏，并确认游戏与小窗权限级别一致。如果游戏使用真正的独占全屏并绕过 DWM，这是 Windows 桌面窗口层级限制，不能在不注入游戏的前提下可靠覆盖。

### 游戏锁定鼠标时视频被意外隐藏

v1.4 只在系统光标真实可见时应用整体透明的移入隐藏，并带 80ms 防抖。游戏正确隐藏/锁定光标时应保持视频可见；若游戏仍显示一个自由光标，按正常移入逻辑隐藏属于预期行为。

### 开启 HAGS 后视频掉帧

复制诊断信息，记录活动视频掉帧计数；随后分别测试自动 GPU、软件兼容模式以及 HAGS 开/关。若只有全屏游戏争用 GPU 时掉帧，通常与 GPU 调度/合成负载有关；若小窗静置也持续增加掉帧，请附诊断提交问题。

### 热键没有响应

检查是否正在输入、是否有热键冲突提示，以及窗口是否隐藏且后台媒体热键关闭。当前测试包启动时应出现 UAC 提示；如果拒绝提权，程序不会启动。

若鼠标侧键无法绑定，请确认鼠标驱动没有把侧键改成键盘宏；默认的“前进/后退”或标准侧键模式可由低级钩子和 Raw Input 双通道识别。绑定成功后按钮应立即显示“鼠标侧键1”或“鼠标侧键2”。

## 12. 关于

- 软件：小窗浏览器 / Mini-Window-Browser
- 版本：1.4.0.0（正式版）
- 技术：C++20、Win32、Direct2D、DirectWrite、WebView2 1.0.4078.44
- 仓库：[https://github.com/azurplain/Mini-Window-Browser](https://github.com/azurplain/Mini-Window-Browser)
- 许可证：GNU General Public License v3.0
