# 小窗浏览器 v1.4.0

v1.4 是一次完整的 UI、窗口模式、热键和媒体控制升级，重点改善单屏游戏环境中的操作效率与稳定性。

## 下载

下载 `Mini-Window-Browser-v1.4.0-x64.zip`，完整解压后运行 `XiaoChuang.exe`。压缩包内已经包含 WebView2 Loader、用户手册、更新日志和第三方许可说明。

从旧版本升级时，请保留自己的 `config.ini` 和 `XiaoChuang.exe.WebView2` 数据目录；v1.4 会兼容旧配置、书签、标签会话和预设。

## 主要更新

### 全新界面

- Direct2D / DirectWrite Fluent 风格界面，支持 Windows 深浅色和 DPI 缩放。
- 新图标覆盖 EXE、任务栏、Alt-Tab、窗口和托盘。
- 标签页自动收窄、内嵌关闭按钮、右键批量关闭；书签改为下拉式访问。

### 更可靠的窗口体验

- 重构 Normal / Maximized / ImmersionHole / ImmersionAutoHide / Hidden 状态机。
- 修复最小化、托盘恢复、隐藏白屏、窗口位置回跳、顶部蓝条和标签画面残留。
- 右上角最大化按钮无缝覆盖工作区；网页视频全屏只覆盖小窗内容区。
- 可选按视频比例调整网页全屏窗口，退出后恢复原位置和大小。

### 沉浸与热键

- 挖孔模式保持圆孔完全透明、孔外不透明；整体透明模式独立提供百分比透明度。
- 点按快进/快退跳转 5 秒，长按进入连续倒退或 2×–5× 快进。
- 新增隐藏时媒体热键、网页与跨程序输入保护。
- 鼠标中键和双侧键使用低级钩子与 Raw Input 双通道，修复管理员环境中的绑定与触发问题。

### 媒体、性能与诊断

- 优先适配 Bilibili、抖音、YouTube，并回退到通用 HTML5 视频。
- 移除常驻 16ms 挖孔轮询，降低全屏游戏与视频同时运行时的额外负载。
- 新增自动 GPU / 软件兼容模式，以及 HAGS、WebView2 Runtime、视频尺寸和掉帧诊断。
- WebView2 SDK 升级到 1.0.4078.44，代码拆分为职责清晰的独立模块。

## 验证结果

- Release x64、x86 `/W4` 构建通过。
- x64、x86 核心测试均为 65/65。
- 20 个 MSVC 静态分析报告均为零缺陷。
- 已验证双侧键绑定与使用、启动热键、主题切换、托盘恢复、地址栏全选和沉浸模式切换。

## 使用提示

- 为兼容管理员游戏和启动器，正式版每次启动都会显示 Windows UAC。
- 真正的独占全屏游戏可能绕过 Windows DWM，普通桌面窗口无法覆盖；建议使用无边框全屏或窗口化全屏。
- 软件兼容渲染模式需要重启程序后生效。

完整说明请查看压缩包中的 `README-使用说明.md`，完整变更记录请查看 `CHANGELOG.md`。
